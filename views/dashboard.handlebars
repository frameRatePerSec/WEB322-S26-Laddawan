<div class="card w-96 bg-base-100 shadow-lg p-6 space-y-6">
  <h2 class="card-title text-primary">Welcome, {{user.username}}!</h2>
  <p class="text-base-content">Email: {{user.email}}</p>
  <div class="flex gap-4">
    <a href="/tasks" class="btn btn-primary">View Tasks</a>
    <a href="/logout" class="btn btn-warning">Logout</a>
  </div>
</div>
